# This program is created to calculate the tip
amount = float(input("Enter the amount: "))
tip = float(input(print("Enter the tip in percentage: ")))
tip_amount=float(amount*(tip/100))
print("Your tip value is" + str(tip_amount))
